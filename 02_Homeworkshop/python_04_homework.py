#1. 이름 공간
#변수를 찾을 떄 접근하는 namespace 를 순서대로 나열

#Local >> Enclosed >> Global >> Built-in

#2. 매개변수와 인자 그리고 반환
#함수에서 return을 작성하지 않으면 Nome 값 반환
#함수의 매개변수(parameter)는 함수를 선언할 때 설정한 값이며,
#전달인자(argument)는 함수를 호출할 때 넘겨 주는 값이다.

#3.재귀 함수의 장단점(반복문과의 차이)
#장점: 변수 사용을 줄일 수 있다.
#단점: 응답속도가 느리다.